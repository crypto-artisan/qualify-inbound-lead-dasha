/**
 * A description of an in-progress operation's state.
 */
export interface ProgressEvent {
    percent: number;
    description: string;
    scopes: string[];
}
export declare type ProgressHandler = (event: ProgressEvent) => void;
