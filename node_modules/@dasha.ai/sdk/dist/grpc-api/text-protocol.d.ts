import * as base from "./base";
import * as errors from "../errors";
import { Account } from "../account";
import { Logger } from "../log";
import * as sdk_grpc_pb from "./generated/sdk_grpc_pb";
export declare type MessageStream = base.Stream<{
    error: [error: errors.Error];
    text: [jobId: string, text: string];
    close: [jobId: string];
}>;
export declare class TextProtocolClient extends base.Client<sdk_grpc_pb.TextProtocolClient> {
    constructor(account: Account, log: Logger);
    sendText(request: {
        jobId: string;
        text: string;
    }): Promise<void>;
    sendClose(request: {
        jobId: string;
    }): Promise<void>;
    messageStream(request: {
        jobId: string;
    }): MessageStream;
}
