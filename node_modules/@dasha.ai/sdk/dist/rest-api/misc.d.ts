import { Account } from "../account";
import { Logger } from "../log";
import * as base from "./base";
import * as api from "./generated/api";
export declare class MiscApi extends base.Api<api.MiscApi> {
    constructor(account: Account, log: Logger);
    getAuthServerUrl(): Promise<string>;
    startTrial(): Promise<{
        apiKey: string;
    }>;
    getAudioClientAuthToken(request: {
        customerId: string;
        applicationName: string;
        groupId: string;
    }): Promise<string>;
}
